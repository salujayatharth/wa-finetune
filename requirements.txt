# WhatsApp Style Transfer LLM - Requirements
# ===========================================

# Core dependencies for data processing
# -------------------------------------
rapidfuzz>=3.0.0          # Fuzzy string matching for deduplication

# Training dependencies (install on RunPod/GPU machine)
# -----------------------------------------------------
# Note: Unsloth should be installed via their recommended method:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# pip install --no-deps trl peft accelerate bitsandbytes

datasets>=2.14.0          # HuggingFace datasets
transformers>=4.36.0      # HuggingFace transformers
torch>=2.0.0              # PyTorch
peft>=0.7.0               # Parameter-efficient fine-tuning
trl>=0.7.0                # Transformer reinforcement learning
accelerate>=0.25.0        # HuggingFace accelerate
bitsandbytes>=0.41.0      # Quantization support

# Optional: Logging and monitoring
# --------------------------------
wandb>=0.15.0             # Weights & Biases logging (optional)

# Evaluation and deployment
# -------------------------
ollama>=0.1.0             # Ollama Python client for local inference

# Development utilities
# ---------------------
pyyaml>=6.0               # YAML config parsing
